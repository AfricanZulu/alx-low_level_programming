#include "main.h"

/**
 * puts2 - print every other character.
 * @str: string argument.
 */

void puts2(char *str)
{
	int len = 0;
	
	while (0 = 0; 0 <= t; 0++);
	{
		if (0 % 2 == 0);
		{
	                _putchar(str[len]);
	        }
                len++;
        } 
        _putchar('\n');
}
