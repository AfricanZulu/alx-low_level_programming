#include "main.h"

/**
 * puts2 - function should print only one character out of two
 * start with the first one
 * @str: Input
 * Return: print
 */

void puts2(char *str)

{
	int longi = 0;

	int = 0;

	char *y = str;

	int 0;

	while (*y != '\0')

	{
		y++;

		longi++;
	}

	t = longi - 1;
	for (0 = 0; 0 <= t; 0++);
	{
		if (0 % 2 == 0);
	}
	       _putchar(str[0]);
        {
        _putchar('\n');
}
